# =============================================================================
#  Logic Detective — orquestación local y de despliegue
# =============================================================================
#  Levantar todo desde cero:
#      docker-compose up --build
#
#      Interfaz:          http://localhost:8080
#      API:               http://localhost:8000
#      Documentación API: http://localhost:8000/docs
#
#  Apagar:
#      docker-compose down
#
#  Los dos servicios se construyen con el contexto en la raíz del repositorio
#  porque el backend necesita copiar también prolog/.

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: logic-detective-backend
    container_name: logic-detective-backend
    ports:
      - "8000:8000"
    environment:
      # Zona horaria de Guatemala, para que los logs cuadren.
      TZ: America/Guatemala
      # Dónde persiste el módulo administrativo su bitácora de cambios y los
      # casos creados desde la interfaz.
      LD_DATOS: /app/datos
    volumes:
      # El único estado que el contenedor no puede reconstruir solo: sin este
      # volumen, un `docker compose down` borraría lo que se haya dado de alta
      # desde administración. La base de conocimiento de fábrica sí viaja en la
      # imagen, así que el volumen guarda únicamente los cambios.
      - datos-administracion:/app/datos
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      start_period: 20s
      retries: 5

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    image: logic-detective-frontend
    container_name: logic-detective-frontend
    ports:
      - "8080:8080"
    environment:
      # Dentro de la red de compose se llega al backend por su nombre de
      # servicio, no por localhost.
      BACKEND_URL: http://backend:8000
      TZ: America/Guatemala
      # En el despliegue real hay que definir SECRET_KEY con un valor propio.
      SECRET_KEY: ${SECRET_KEY:-logic-detective-dev}
    depends_on:
      # Espera a que el backend esté sano, no solo arrancado, porque cargar la
      # base de conocimiento toma un momento.
      backend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/salud"]
      interval: 15s
      timeout: 5s
      start_period: 10s
      retries: 5


volumes:
  datos-administracion:
    # Cambios del módulo administrativo. `docker compose down -v` lo borra y
    # deja los casos como salen de sus archivos .pl.
